<?xml version="1.0" encoding="utf-8"?>
<layout>

    <data>
        <variable
            name="createNoteDialogViewModel"
            type="com.yeonkims.realnoteapp.logic.viewmodels.CreateNoteDialogViewModel" />
    </data>

    <LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:tools="http://schemas.android.com/tools"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="center"
        android:minWidth="300dp"
        android:orientation="vertical"
        android:padding="20dp">

        <TextView
            android:id="@+id/confirmTextView"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:gravity="center"
            android:textAppearance="@style/TextAppearance.AppCompat.Large"
            android:text="New note" />

        <EditText
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:gravity="top|left"
            android:hint="Write note"
            android:text="@={createNoteDialogViewModel.newNote}"
            android:inputType="textMultiLine"
            android:lines="10"
            android:maxLines="15"
            android:minLines="5" />


        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="20dp"
            android:orientation="horizontal">

            <Button
                android:id="@+id/cancelBtn"
                android:layout_width="0dp"
                android:layout_height="48dp"
                android:layout_marginEnd="7dp"
                android:layout_weight="1"
                android:text="Cancel"
                android:textSize="14sp" />

            <Button
                android:id="@+id/okBtn"
                android:layout_width="0dp"
                android:layout_height="48dp"
                android:layout_weight="1"
                android:enabled="@{createNoteDialogViewModel.createIsEnabled}"
                android:text="OK"
                android:textSize="14sp" />

        </LinearLayout>

    </LinearLayout>
</layout>